---
phase: 04-tyre-strategy
plan: 02
type: execute
---

<objective>
Add degradation rate annotations to the tyre strategy chart and comprehensive tests for StintAccumulator.

Purpose: Complete the tyre strategy insight with degradation visibility and ensure correctness of stint tracking logic.
Output: Degradation annotations on stint bars + passing tests for StintAccumulator.
</objective>

<execution_context>
~/.claude/get-your-work-done/workflows/execute-phase.md
~/.claude/get-your-work-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior plan:
@.planning/phases/04-tyre-strategy/04-01-SUMMARY.md

# Key source files:
@src/insights/tyre_strategy_window.py
@src/bayesian_tyre_model.py
@tests/test_lap_time_evolution.py

**Tech stack available:** PySide6, Matplotlib (QtAgg backend), numpy
**Established patterns:**
- Pure-data accumulator tested without Qt (LapTimeAccumulator test pattern)
- BayesianTyreDegradationModel has per-compound degradation rates in tyre_profiles dict
- Compound degradation priors: HARD=0.01, MEDIUM=0.03, SOFT=0.05, INTERMEDIATE=0.04, WET=0.02 s/lap
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add degradation rate annotations to stint bars</name>
  <files>src/insights/tyre_strategy_window.py</files>
  <action>
Enhance the _redraw method in TyreStrategyWindow to show degradation information:

1. **Degradation rate display per stint:**
   - Below each stint bar, show the expected degradation rate from the Bayesian model's default profiles
   - Use a static dict mapping compound_int to deg rate: {0: 0.05, 1: 0.03, 2: 0.01, 3: 0.04, 4: 0.02}
   - Format: small text below bar showing "↓{rate:.2f}s/lap" (e.g., "↓0.05s/lap" for SOFT)
   - Text color: #AAAAAA (subtle gray), fontsize=7
   - Only show if stint is at least 3 laps long (avoid clutter on short stints)

2. **Stint duration label refinement:**
   - Inside each bar: "{compound_letter} ({num_laps})" e.g., "S (12)" for 12-lap soft stint
   - If bar is too narrow (stint < 4 laps), show just the compound letter

3. **Add a compound legend:**
   - Below the chart, add a horizontal legend showing colored patches for each compound
   - Use fig.text() or a secondary axes for the legend row
   - Show: ■ SOFT ■ MEDIUM ■ HARD ■ INTER ■ WET with appropriate colors

Do NOT import or instantiate BayesianTyreDegradationModel — just use the static degradation rate dict. The Bayesian model requires a full FastF1 DataFrame which isn't available in the stream context.
  </action>
  <verify>python -c "from src.insights.tyre_strategy_window import TyreStrategyWindow; print('Import OK')"</verify>
  <done>Stint bars show degradation rate annotations, compound legend visible, no import of BayesianTyreDegradationModel</done>
</task>

<task type="auto">
  <name>Task 2: Add tests for StintAccumulator</name>
  <files>tests/test_tyre_strategy.py</files>
  <action>
Create test file following the test_lap_time_evolution.py pattern — test StintAccumulator directly (no Qt needed):

1. **TestStintDetection:**
   - Feed frames with constant compound → single stint, end_lap updates each frame
   - Feed frames where compound changes from 0(SOFT) to 1(MEDIUM) at lap 10 → two stints
   - Verify first stint: compound=0, start_lap=1, end_lap=9
   - Verify second stint: compound=1, start_lap=10, end_lap=current

2. **TestMultipleStints:**
   - Feed frames for 3-stint strategy: SOFT(1-8), MEDIUM(9-25), HARD(26-50)
   - Verify 3 stints with correct boundaries

3. **TestMultiDriverStints:**
   - Feed frames for 2 drivers with different strategies
   - Verify stints accumulate independently per driver

4. **TestEdgeCases:**
   - None compound in frame → skip gracefully, don't create stint
   - Single lap stint (immediate pit after 1 lap) → stint with start_lap == end_lap
   - First frame has lap > 1 → stint starts at that lap

5. **TestHelperMethods:**
   - get_all_drivers() returns sorted list
   - get_max_lap() returns highest lap across all drivers
   - get_stints() returns empty list for unknown driver

Helper: Create a `_make_frame(drivers_data, t=0.0)` helper that builds a frame dict matching the stream format:
```python
def _make_frame(drivers: dict[str, dict], t: float = 0.0) -> dict:
    return {
        "frame": {
            "t": t,
            "drivers": drivers,
        },
        "track_status": "GREEN",
    }
```

Run: `python -m pytest tests/test_tyre_strategy.py -v`
  </action>
  <verify>python -m pytest tests/test_tyre_strategy.py -v</verify>
  <done>All tests pass, covering stint detection, multi-stint, multi-driver, edge cases, and helper methods</done>
</task>

<task type="auto">
  <name>Task 3: Run full test suite</name>
  <files></files>
  <action>
Run the complete test suite to verify no regressions:
```
python -m pytest tests/ -v
```

Verify all existing tests (104 from Phase 3) plus new tyre strategy tests pass.
Fix any regressions if found.
  </action>
  <verify>python -m pytest tests/ -v</verify>
  <done>All tests pass (existing 104 + new tyre strategy tests), no regressions</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m pytest tests/ -v` — all tests pass (existing + new)
- [ ] `python -c "from src.insights.tyre_strategy_window import TyreStrategyWindow"` — no import errors
- [ ] Stint bars show degradation rate annotations
- [ ] Compound legend visible on chart
- [ ] Phase 4 complete
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Degradation rates displayed on stint bars
- Compound legend on chart
- All StintAccumulator tests pass
- No regressions in existing tests
- Phase 4 complete
</success_criteria>

<output>
After completion, create `.planning/phases/04-tyre-strategy/04-02-SUMMARY.md`
</output>

## Confidence Summary

**Overall: 91%** (High)

| Task | Confidence | Notes |
|------|------------|-------|
| T1: Degradation annotations | 88% | Text positioning on broken_barh may need iteration |
| T2: StintAccumulator tests | 95% | Follows test_lap_time_evolution.py pattern exactly |
| T3: Full test suite | 98% | Standard verification run |

**Factors increasing confidence:**
- Static degradation rates (no Bayesian model complexity)
- Test pattern well-established from Phase 3
- StintAccumulator is simpler than LapTimeAccumulator

**Factors slightly reducing confidence:**
- Matplotlib text positioning on narrow bars may need fine-tuning
- Legend placement below chart requires careful layout
