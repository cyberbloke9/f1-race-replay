---
phase: 01-foundation-testing
plan: 02
type: execute
---

<objective>
Test the lib/ utility modules — time parsing/formatting, tyre compound mappings, and settings manager.

Purpose: These utilities are used throughout the codebase. Verifying their correctness catches edge cases before building on them in later phases.
Output: Three test files covering all public functions in lib/time.py, lib/tyres.py, and lib/settings.py.
</objective>

<execution_context>
~/.claude/get-your-work-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-testing/01-RESEARCH.md

@src/lib/time.py
@src/lib/tyres.py
@src/lib/settings.py

**Depends on:** Plan 01-01 (pytest setup + conftest.py)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tests for time.py and tyres.py</name>
  <files>tests/test_time.py, tests/test_tyres.py</files>
  <action>
    **tests/test_time.py:**
    Test `parse_time_string` with parametrize covering:
    - Standard formats: "01:26.123" -> 86.123, "01:26" -> 86.0
    - Four-part: "00:01:26:123000" -> 86.123, "00:01:26.123000" -> 86.123
    - Timedelta format: "0 days 00:01:27.060000" -> 87.06
    - Zero: "00:00.000" -> 0.0
    - Edge: "" -> None, None -> None
    - Large values: "01:30:00.000000" -> 5400.0

    Test `format_time` with parametrize covering:
    - Normal: 86.123 -> "01:26.123"
    - Zero: 0.0 -> "00:00.000"
    - Large: 5400.0 -> "90:00.000"
    - Edge: None -> "N/A", -1.0 -> "N/A"

    **tests/test_tyres.py:**
    Test `get_tyre_compound_int`:
    - All 5 compounds: SOFT->0, MEDIUM->1, HARD->2, INTERMEDIATE->3, WET->4
    - Case insensitive: "soft"->0, "Soft"->0
    - Unknown: "UNKNOWN_COMPOUND" -> -1

    Test `get_tyre_compound_str`:
    - Reverse mappings: 0->"SOFT", 1->"MEDIUM", 2->"HARD", 3->"INTERMEDIATE", 4->"WET"
    - Unknown: -1->"UNKNOWN", 99->"UNKNOWN"
  </action>
  <verify>Run `pytest tests/test_time.py tests/test_tyres.py -v` — all tests pass</verify>
  <done>All parametrized tests pass for time parsing/formatting and tyre compound mappings. Edge cases covered.</done>
</task>

<task type="auto">
  <name>Task 2: Tests for settings.py</name>
  <files>tests/test_settings.py</files>
  <action>
    Test `SettingsManager` using the `tmp_settings` and `reset_settings_singleton` fixtures from conftest.py.

    Tests to write:
    1. **test_default_values** — Fresh instance has correct defaults (cache_location=".fastf1-cache", computed_data_location="computed_data")
    2. **test_get_set** — `settings.set("key", "value")` then `settings.get("key")` returns "value"
    3. **test_save_load_roundtrip** — Set values, save(), create new instance (reset singleton first), load() — values persist
    4. **test_property_accessors** — `settings.cache_location` and `settings.computed_data_location` return defaults. Set via property setter, verify getter returns new value.
    5. **test_reset_to_defaults** — Set custom values, call `reset_to_defaults()`, verify defaults restored
    6. **test_singleton_pattern** — Two calls to `get_settings()` return same instance (use `is` comparison)
    7. **test_corrupted_settings_file** — Write invalid JSON to settings file path, create instance — should fall back to defaults without crashing
    8. **test_get_with_default** — `settings.get("nonexistent", "fallback")` returns "fallback"

    For isolation: patch `SettingsManager._get_settings_file_path` to return `tmp_path / "test_settings.json"` in a fixture. The autouse `reset_settings_singleton` from conftest ensures clean state.
  </action>
  <verify>Run `pytest tests/test_settings.py -v` — all 8 tests pass</verify>
  <done>SettingsManager fully tested — defaults, persistence, singleton, error handling, properties all verified</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/test_time.py tests/test_tyres.py tests/test_settings.py -v` — all pass
- [ ] No warnings about unclosed files or deprecated features
- [ ] Tests run in < 5 seconds (no network, no heavy I/O)
</verification>

<success_criteria>
- All public functions in time.py, tyres.py, settings.py have test coverage
- Edge cases covered (None, empty, invalid, boundary values)
- Settings singleton properly isolated between tests
- All tests pass
</success_criteria>

## Confidence Summary

**Overall: 95%** (High)

| Task | Confidence | Notes |
|------|------------|-------|
| T1: time.py + tyres.py tests | 97% | Pure functions, clear input/output contracts |
| T2: settings.py tests | 92% | Singleton reset pattern from RESEARCH.md, tmp_path isolation |

**Factors:** Utilities are simple pure functions. Settings singleton is the only tricky part — well-documented in research.

<output>
After completion, create `.planning/phases/01-foundation-testing/01-02-SUMMARY.md`
</output>
