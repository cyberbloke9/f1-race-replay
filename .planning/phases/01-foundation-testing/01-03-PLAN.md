---
phase: 01-foundation-testing
plan: 03
type: execute
---

<objective>
Test the f1_data.py data pipeline — telemetry extraction, resampling, and frame building.

Purpose: f1_data.py is the heart of the application. Testing its data pipeline (driver telemetry processing, NumPy resampling at 25 FPS, multiprocessing orchestration) ensures the foundation is solid before building insights on top of it.
Output: `tests/test_f1_data.py` with comprehensive tests for the data pipeline functions.
</objective>

<execution_context>
~/.claude/get-your-work-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-testing/01-RESEARCH.md

@src/f1_data.py

**Depends on:** Plan 01-01 (conftest fixtures — mock_session, mock_laps, mock_telemetry_df, mock_lap)

**Key patterns from f1_data.py:**
- `_process_single_driver(args)` takes a tuple `(driver_no, session, driver_code)` — must be top-level for multiprocessing pickle
- Returns dict with keys: driver_code, t, x, y, race_distance, rel_distance, lap_number, tyre_compound, tyre_life, speed, gear, drs, throttle, brake, max_lap
- `get_race_telemetry(session)` uses `multiprocessing.Pool` to parallelize, then resamples all drivers to a unified 25 FPS timeline
- Resampling uses `np.interp` on each channel
- Pickle caching reads/writes to `computed_data/` directory

**Constraints:**
- Mock `multiprocessing.Pool` to run serially in tests
- Mock `fastf1.Cache.enable_cache` — never touch real cache
- Use `tmp_path` for any file I/O (pickle cache)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test _process_single_driver telemetry extraction</name>
  <files>tests/test_f1_data.py</files>
  <action>
    Create `tests/test_f1_data.py`. Read f1_data.py first to understand the exact return structure.

    Tests for `_process_single_driver`:

    1. **test_process_single_driver_returns_correct_structure** — Call with mock session fixture. Verify returned dict has all expected keys: driver_code, t, x, y, race_distance, rel_distance, lap_number, tyre_compound, tyre_life, speed, gear, drs, throttle, brake, max_lap. Verify all array values are numpy arrays.

    2. **test_process_single_driver_concatenates_laps** — Provide 3 laps of telemetry. Verify the output arrays are longer than any single lap's telemetry (concatenation happened). Verify lap_number array contains values [1, 1, ..., 2, 2, ..., 3, 3, ...].

    3. **test_process_single_driver_race_distance_accumulates** — Verify race_distance is monotonically increasing across laps (total_dist_so_far accumulates). First lap starts near 0, second lap starts where first ended.

    4. **test_process_single_driver_empty_laps_returns_none** — Mock session where `pick_drivers` returns empty DataFrame. Verify function returns None.

    5. **test_process_single_driver_tyre_compound_mapping** — Verify tyre_compound array contains correct integer values (SOFT=0, MEDIUM=1, etc.) matching the input lap compounds.

    For mocking: build the args tuple `(driver_no, mock_session, driver_code)` using conftest fixtures. The mock_session's laps must support `.pick_drivers()` and `.iterlaps()` as the real code calls them.
  </action>
  <verify>Run `pytest tests/test_f1_data.py -v -k "process_single_driver"` — all 5 tests pass</verify>
  <done>_process_single_driver tested for structure, concatenation, distance accumulation, empty case, and tyre mapping</done>
</task>

<task type="auto">
  <name>Task 2: Test enable_cache and pickle caching logic</name>
  <files>tests/test_f1_data.py (append)</files>
  <action>
    Add tests to the same file:

    1. **test_enable_cache_calls_fastf1** — Use `mocker.patch("src.f1_data.fastf1.Cache.enable_cache")` and `mocker.patch("src.f1_data.get_settings")` to return a mock with `cache_location="/tmp/test-cache"`. Call `enable_cache()`. Verify `fastf1.Cache.enable_cache` was called with the settings path. Also patch `os.makedirs` if path doesn't exist.

    2. **test_enable_cache_creates_directory** — Patch os.path.exists to return False, patch os.makedirs. Verify makedirs is called with the cache path.

    3. **test_fps_constant** — Simple assertion: `from src.f1_data import FPS, DT; assert FPS == 25; assert DT == 1/25`

    Note: Do NOT test `get_race_telemetry` end-to-end here — that requires mocking Pool, resampling logic, and is too heavy. The per-driver extraction in Task 1 covers the core logic. End-to-end integration can come in a future phase if needed.
  </action>
  <verify>Run `pytest tests/test_f1_data.py -v` — all tests pass (5 from Task 1 + 3 from Task 2 = 8 total)</verify>
  <done>enable_cache tested with mocked FastF1 and settings. FPS constant verified. 8 total tests in test_f1_data.py.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/test_f1_data.py -v` — all 8 tests pass
- [ ] No tests depend on real FastF1 cache or network
- [ ] Tests run in < 5 seconds
</verification>

<success_criteria>
- _process_single_driver thoroughly tested with mock session data
- enable_cache tested with proper mocking
- All assertions meaningful (not just "doesn't crash")
- No flaky tests (deterministic mock data)
</success_criteria>

## Confidence Summary

**Overall: 82%** (High)

| Task | Confidence | Notes |
|------|------------|-------|
| T1: _process_single_driver | 78% | Mocking iterlaps() and get_telemetry() chain needs precise shape matching |
| T2: enable_cache + constants | 92% | Simple patching, straightforward assertions |

**Factors:** The mock chain (session.laps.pick_drivers().iterlaps() → lap.get_telemetry()) is the trickiest part. May need iteration to get the mock shape right.

<output>
After completion, create `.planning/phases/01-foundation-testing/01-03-SUMMARY.md`
</output>
