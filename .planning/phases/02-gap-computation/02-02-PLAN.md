---
phase: 02-gap-computation
plan: 02
type: execute
---

<objective>
Fix the distance accumulation bug and integrate the gap engine into the race telemetry pipeline so every frame carries pre-computed time-based gaps.

Purpose: Make gap data flow through the existing data pipeline so the leaderboard and future insights can use accurate gaps.
Output: Fixed distance accumulation in `_process_single_driver`, gap data in every frame's driver dict.
</objective>

<execution_context>
~/.claude/get-your-work-done/workflows/execute-phase.md
~/.claude/get-your-work-done/templates/phase-prompt.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-gap-computation/02-01-SUMMARY.md
@src/f1_data.py
@src/lib/gaps.py

**Deferred issue from Phase 1:**
- `total_dist_so_far` in `_process_single_driver` (line 60) is initialized to 0.0 but never incremented after each lap. Fix: after appending lap data, add `total_dist_so_far += d_lap.max()` (max distance within the lap = one lap length).

**Integration point:**
- In `get_race_telemetry()`, after resampling all drivers (step 3), call `compute_driver_gaps(resampled_data, timeline)` to get per-driver gap arrays.
- In the frame-building loop (step 5), add `gap_to_leader` and `gap_to_ahead` to each driver's frame_data dict.
- The stream protocol already sends arbitrary driver dict fields — no protocol changes needed.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix distance accumulation bug in _process_single_driver</name>
  <files>src/f1_data.py</files>
  <action>
    In `_process_single_driver()`, after line 99 (the `brake_all.append(brake_lap)` line), add:
    ```
    total_dist_so_far += d_lap.max()
    ```
    This ensures `race_d_lap = total_dist_so_far + d_lap` properly accumulates across laps, giving each subsequent lap a higher base distance.

    Do NOT change anything else in this function. The fix is exactly one line.
  </action>
  <verify>python -m pytest tests/test_f1_data.py -v (existing tests still pass — the test_race_distance_present_and_valid test documented this as a known bug)</verify>
  <done>total_dist_so_far increments by d_lap.max() after each lap. Race distance now accumulates correctly across laps.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate gap engine into get_race_telemetry frame building</name>
  <files>src/f1_data.py</files>
  <action>
    1. Add import at top of f1_data.py:
       `from src.lib.gaps import compute_driver_gaps`

    2. After the resampling loop (after step 3, around line 288), add:
       ```
       # 3.1 Compute time-based gaps for all drivers
       gap_data = compute_driver_gaps(resampled_data, timeline)
       ```

    3. In the frame-building loop (step 5, inside the `for idx, car in enumerate(snapshot):` block around line 407), add gap fields to frame_data[code]:
       ```
       # Add pre-computed gap data
       driver_gap = gap_data.get(code)
       if driver_gap is not None:
           frame_data[code]["gap_to_leader"] = float(driver_gap["gap_to_leader"][i]) if not np.isnan(driver_gap["gap_to_leader"][i]) else None
           frame_data[code]["gap_to_ahead"] = float(driver_gap["gap_to_ahead"][i]) if not np.isnan(driver_gap["gap_to_ahead"][i]) else None
       else:
           frame_data[code]["gap_to_leader"] = None
           frame_data[code]["gap_to_ahead"] = None
       ```

    Note: The `i` index used in frame building corresponds to the timeline index, which matches the gap_data arrays since both use the same timeline.
  </action>
  <verify>python -c "from src.f1_data import get_race_telemetry; print('Import OK')" should succeed without errors. Existing tests: python -m pytest tests/ -v</verify>
  <done>Gap engine imported and called in get_race_telemetry. Each frame's driver dict contains gap_to_leader and gap_to_ahead fields (float seconds or None).</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m pytest tests/ -v` — all existing tests pass
- [ ] `total_dist_so_far` line added after brake_all.append
- [ ] `compute_driver_gaps` import and call present in f1_data.py
- [ ] Frame driver dicts contain gap_to_leader and gap_to_ahead keys
</verification>

<success_criteria>
- All tasks completed
- All existing tests pass (no regressions)
- Distance accumulation bug fixed (1 line)
- Gap data flows through pipeline into frames
</success_criteria>

## Confidence Summary

**Overall: 90%** (High)

| Task | Confidence | Notes |
|------|------------|-------|
| T1: Fix distance bug | 98% | One-line fix, well-understood |
| T2: Pipeline integration | 85% | Standard integration, but frame loop index mapping needs care |

**Factors:**
- ✅ Distance fix is trivial and well-documented
- ✅ Integration follows existing patterns in get_race_telemetry
- ⚠️ Need to verify timeline index `i` aligns correctly between frame loop and gap arrays
- ✅ No new dependencies

<output>
After completion, create `.planning/phases/02-gap-computation/02-02-SUMMARY.md`
</output>
